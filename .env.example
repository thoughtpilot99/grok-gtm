# From Overloop → Settings → API Keys. Sent as the Authorization header.
OVERLOOP_API_KEY=

# From max → Settings → API Keys. Sent as Authorization: Bearer.
MAX_API_KEY=

# Optional overrides
# OVERLOOP_API_URL=https://api.overloop.ai
# MAX_API_URL=https://api.yourmax.ai
